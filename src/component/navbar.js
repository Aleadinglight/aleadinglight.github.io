import React, { Component } from 'react';
import { Nav } from 'react-bootstrap';

class Navbar extends Component {
    state = {  }
    render() { 
        return ( 
            <Nav navbar-expand-lg nav-scroll>
            </Nav>
        );
    }
}
 
export default Navbar;